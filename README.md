# setuptools-scm-distance

setuptools-scm-distance is a Python 3.10+ setuptools-scm plugin that produces dynamic versions for pyproject.toml packages based on [setupmeta's distance strategy.](https://github.com/codrsquad/setupmeta/blob/main/docs/versioning.rst#distance)